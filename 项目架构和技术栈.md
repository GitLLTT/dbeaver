我来帮你了解这个项目的架构和技术栈。让我先探索一下项目结构。
让我再看一下插件和核心模块的结构：
根据我的分析，这是 **DBeaver** 项目 —— 一个知名的开源数据库管理工具。以下是项目的技术栈和架构：

---

## 技术栈

### 核心技术
| 技术 | 说明 |
|------|------|
| **Java** | 主要开发语言，需要 JDK 21 |
| **OSGi** | 插件和依赖管理平台 |
| **Eclipse RCP** | 富客户端应用框架，用于构建桌面 UI |
| **Maven + Tycho** | 构建工具，Tycho 用于构建 Eclipse 插件 |
| **JDBC** | 数据库连接标准 API |

### 关键库
- **JSQLParser** / **Antlr4** - SQL 语法解析
- **SSHJ** - SSH 隧道连接
- **Apache POI** - Office 文件处理
- **JFreeChart** - 图表绘制
- **JTS** - 地理空间数据处理
- **Apache JEXL** - 表达式语言

---

## 项目架构

```
dbeaver/
├── plugins/          # 130+ OSGi 插件模块
│   ├── org.jkiss.dbeaver.core/       # 核心模块
│   ├── org.jkiss.dbeaver.model*/     # 数据模型层（与 UI 分离）
│   ├── org.jkiss.dbeaver.ui*/        # UI 层
│   ├── org.jkiss.dbeaver.ext.*/      # 数据库扩展插件
│   └── org.jkiss.dbeaver.net.*/      # 网络模块
├── features/         # Eclipse Feature 打包单元
├── product/          # 产品构建配置
├── test/             # 测试模块
└── docs/             # 文档
```

### 模块设计特点
1. **Model/UI 分离** - 数据模型插件与桌面 UI 插件分离，这样后端插件可以同时用于 DBeaver 桌面版和 [CloudBeaver](https://github.com/dbeaver/cloudbeaver) Web 版

2. **数据库驱动插件化** - 每个数据库类型都是独立插件：
   - `org.jkiss.dbeaver.ext.mysql`
   - `org.jkiss.dbeaver.ext.postgresql`
   - `org.jkiss.dbeaver.ext.oracle`
   - `org.jkiss.dbeaver.ext.mssql` 
   - ... 支持 100+ 数据库

3. **跨平台支持**：
   - Windows (x86_64, aarch64)
   - Linux (x86_64, aarch64)
   - macOS (x86_64, aarch64)

---

## 支持的数据库
开箱即用支持 MySQL、MariaDB、Oracle、DB2、PostgreSQL、SQL Server、SQLite、DuckDB、ClickHouse 等 100+ 数据库驱动，并且支持任何有 JDBC/ODBC 驱动的数据库。



org.jkiss.dbeaver.ui.ai 代码结构

src/org/jkiss/dbeaver/ui/ai/
├── AIPropertyTester.java         ← 条件表达式测试器（判断AI是否已配置、是否启用等）
├── AIUIUtils.java                ← UI工具类（弹窗提示、错误处理等）
├── AIWorkbenchInitializer.java   ← 工作台初始化器（启动时注册AI相关内容）
│
├── chat/                         ← AI Chat 面板（我们改造的核心）
│   ├── AIChatPanel.java          ← ★ Chat面板UI + 消息发送逻辑（已改为调用Ollama）
│   ├── AIChatEditorAddIn.java    ← ★ SQL编辑器插件，负责在编辑器里显示/隐藏Chat面板（已改默认开启）
│   └── AIChatViewSwitchHandler.java ← 工具栏"切换AI Chat"按钮的命令处理器
│
├── controls/                     ← 通用UI控件
│   ├── ScopeSelectorControl.java  ← 作用域选择器控件（选择数据库/表的上下文范围）
│   └── ScopeSelectorDialog.java   ← 作用域选择对话框
│
├── engine/                       ← AI 引擎配置页面
│   ├── copilot/
│   │   ├── CopilotConfigurator.java   ← GitHub Copilot 引擎配置UI
│   │   └── CopilotMessages*.java      ← Copilot相关国际化文本
│   └── openai/
│       └── OpenAiConfigurator.java    ← OpenAI 引擎配置UI（API Key、模型选择等）
│
├── format/
│   └── DefaultFormattingConfigurator.java ← SQL格式化配置器（AI生成的SQL的格式化规则）
│
├── internal/                     ← 插件内部基础设施
│   ├── AIConfigurationHandler.java  ← "打开AI设置"命令处理器
│   ├── AITranslateHandler.java      ← "AI Assistant"按钮命令处理器（自然语言转SQL）
│   ├── AIUIActivator.java           ← OSGi Bundle 激活器（插件入口）
│   ├── AIUIFeatures.java            ← 功能开关标记
│   └── AIUIMessages*.java           ← 国际化文本（英/葡/罗/乌克兰语）
│
├── legacy/                       ← 旧版AI补全（兼容保留）
│   ├── AILegacyTranslator.java      ← 旧版自然语言→SQL转换逻辑
│   ├── AISuggestionPopup.java       ← 旧版弹出建议框
│   └── InMemoryHistoryManager.java  ← 内存历史记录管理
│
├── model/                        ← AI模型选择UI组件
│   ├── CachedValue.java             ← 缓存值包装器
│   ├── ContextWindowSizeField.java  ← 上下文窗口大小设置字段
│   └── ModelSelectorField.java      ← 模型选择下拉框组件
│
└── preferences/                  ← AI 偏好设置页面
    ├── AIPreferencePageMain.java        ← 主设置页（启用/禁用AI）
    ├── AIPreferencePageConfiguration.java ← AI配置页
    ├── AIPreferencePageEngines.java     ← 引擎列表设置页
    ├── AIPreferencePagePrompts.java     ← 提示词设置页
    ├── AIPreferencesInitializer.java    ← 设置项默认值初始化
    └── AIIObjectPropertyConfigurator.java ← 对象属性配置器